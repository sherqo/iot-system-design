# How to Generate the PDF Report

just run `make` in the `docs/report` directory to generate the PDF report for Phase 1.
The generated PDF will be saved in the `docs/report/output` directory

## Prerequisites

Make sure you have the following installed on your system:

- LaTeX distribution (e.g., TeX Live, MiKTeX)
- Make utility (usually comes with LaTeX distributions)
